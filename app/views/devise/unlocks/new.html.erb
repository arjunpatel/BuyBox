<%= devise_error_messages! %>

<div class="page-header">
  <div class="second_header">Resend unlock instructions</div>
</div>

<div class="container-fluid content_padding">
  <%= form_for(resource, :as => resource_name, :url => unlock_path(resource_name), :html => {:method => :post}) do |f| %>

      <%= f.email_field :email, :placeholder => "Email" %></div>

      <div><%= f.submit "Resend unlock instructions", :class=> "btn btn-primary" %></div>
  <% end %>
  </div>
